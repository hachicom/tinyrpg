class EventController extends Phaser.GameObjects.Group {
  constructor (config) {
      super(config.scene);

      //TODO: usar esta prefab para controlar todas as falas e eventos de role playing
  }
}